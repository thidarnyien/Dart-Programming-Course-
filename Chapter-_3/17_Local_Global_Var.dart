//Variable Scope ( local & global )

// var ( global & block scope )

var name = "Thidar Nyein"; // global variable // you can call anywhere this var

void main() {
  var age = 20; //local variable ->  block scope {}
  print(name);
}
